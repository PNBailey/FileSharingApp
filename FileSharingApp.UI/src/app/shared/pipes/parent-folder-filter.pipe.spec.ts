import { ParentFolderFilterPipe } from './parent-folder-filter.pipe';

describe('ParentFolderFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ParentFolderFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
